<!DOCTYPE html>
<html>
  <head>
    <title>Amusement Park</title>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= analytics_init if Rails.env.production? %>
    <link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" rel="stylesheet"></link>
  </head>
  <body>
    <!--  navbar -->
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="<%= root_url %>"/>Home</a>
          <% if is_user? %>
            <a class="navbar-brand" href="<%= user_url(current_user) %>"/>Hello, <%= current_user.name %>!</a>
            <% if is_admin? %>
              <a class="navbar-brand" href="<%= root_url %>"/>ADMIN</a>
            <% end %>
            <a class="navbar-brand" href="<%= attractions_url %>"/>Attractions</a>
            <a id="Log Out" class="navbar-brand" href="<%= signout_url %>" data-method="delete"/>Log Out</a>
            <% else %>
            <a class="navbar-brand" href="<%= new_user_url %>"/>Sign Up</a>
            <a class="navbar-brand" href="<%= signin_url %>" />Sign In</a>
          <% end %>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
           <!--  nav here -->
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>

    <!-- content -->
    <div class="container"  role="main">
      <%= yield %>
    </div>

  </body>
</html>
